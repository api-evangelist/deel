naftiko: 1.0.0-alpha2
info:
  label: EOR Worker Endpoints — subpackage_eorWorkerAccounts
  description: 'EOR Worker Endpoints — subpackage_eorWorkerAccounts. 1 operations. Lead operation: Create EOR Worker. Self-contained
    Naftiko capability covering one Deel business surface.'
  tags:
  - Deel
  - subpackage_eorWorkerAccounts
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DEEL_API_KEY: DEEL_API_KEY
capability:
  consumes:
  - type: http
    namespace: eor-worker-endpoints-subpackage-eorworkeraccounts
    baseUri: https://api.letsdeel.com/rest/v2
    description: EOR Worker Endpoints — subpackage_eorWorkerAccounts business capability. Self-contained, no shared references.
    resources:
    - name: eor-worker
      path: /eor/worker
      operations:
      - name: createworker
        method: POST
        description: Create EOR Worker
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
          description: '## Authentication'
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.DEEL_API_KEY}}'
  exposes:
  - type: rest
    namespace: eor-worker-endpoints-subpackage-eorworkeraccounts-rest
    port: 8080
    description: REST adapter for EOR Worker Endpoints — subpackage_eorWorkerAccounts. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/eor/worker
      name: eor-worker
      description: REST surface for eor-worker.
      operations:
      - method: POST
        name: createworker
        description: Create EOR Worker
        call: eor-worker-endpoints-subpackage-eorworkeraccounts.createworker
        with:
          Authorization: rest.Authorization
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: eor-worker-endpoints-subpackage-eorworkeraccounts-mcp
    port: 9090
    transport: http
    description: MCP adapter for EOR Worker Endpoints — subpackage_eorWorkerAccounts. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: create-eor-worker
      description: Create EOR Worker
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: eor-worker-endpoints-subpackage-eorworkeraccounts.createworker
      with:
        Authorization: tools.Authorization
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
