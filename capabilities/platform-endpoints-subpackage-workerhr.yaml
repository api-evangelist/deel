naftiko: 1.0.0-alpha2
info:
  label: Endpoints — subpackage_workerHr
  description: 'Endpoints — subpackage_workerHr. 1 operations. Lead operation: Get HRX manager for worker. Self-contained
    Naftiko capability covering one Deel business surface.'
  tags:
  - Deel
  - subpackage_workerHr
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DEEL_API_KEY: DEEL_API_KEY
capability:
  consumes:
  - type: http
    namespace: platform-endpoints-subpackage-workerhr
    baseUri: https://api.letsdeel.com/rest/v2
    description: Endpoints — subpackage_workerHr business capability. Self-contained, no shared references.
    resources:
    - name: escalations-workers-me-hrx
      path: /escalations/workers/me/hrx
      operations:
      - name: gethrxmanagerforworker
        method: GET
        description: Get HRX manager for worker
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
          description: '## Authentication'
          required: true
    authentication:
      type: bearer
      token: '{{env.DEEL_API_KEY}}'
  exposes:
  - type: rest
    namespace: platform-endpoints-subpackage-workerhr-rest
    port: 8080
    description: REST adapter for Endpoints — subpackage_workerHr. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/escalations/workers/me/hrx
      name: escalations-workers-me-hrx
      description: REST surface for escalations-workers-me-hrx.
      operations:
      - method: GET
        name: gethrxmanagerforworker
        description: Get HRX manager for worker
        call: platform-endpoints-subpackage-workerhr.gethrxmanagerforworker
        with:
          Authorization: rest.Authorization
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: platform-endpoints-subpackage-workerhr-mcp
    port: 9090
    transport: http
    description: MCP adapter for Endpoints — subpackage_workerHr. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: get-hrx-manager-worker
      description: Get HRX manager for worker
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: platform-endpoints-subpackage-workerhr.gethrxmanagerforworker
      with:
        Authorization: tools.Authorization
      outputParameters:
      - type: object
        mapping: $.
